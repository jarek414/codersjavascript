const doc = document;
doc.addEventListener('DOMContentLoaded', function (){
    buttons = Array.from(doc.querySelectorAll('a.btn.deleteBtn'));
    const tBody = doc.querySelector('tbody');

    buttons.forEach()

});